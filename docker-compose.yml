services:
  backend:
    build: ./backend
    environment:
      # The SQLite DB persists in the volume mounted at /data.
      CHURRASCO_DB_PATH: /data/churrasco.db
    volumes:
      - churrasco-data:/data
    expose:
      - "8080"
    restart: unless-stopped

  frontend:
    build: ./frontend
    depends_on:
      - backend
    ports:
      # The app is reachable at http://localhost:8080
      - "8080:80"
    restart: unless-stopped

volumes:
  churrasco-data:
