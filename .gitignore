# --- Java / Maven ---
target/
*.class

# --- Node / Vite ---
node_modules/
dist/
npm-debug.log*
yarn-error.log*

# --- SQLite (the DB isn't versioned; the data/ folder is, via .gitkeep) ---
*.db
*.db-wal
*.db-shm

# --- Environment / secrets ---
.env
.env.*

# --- Local-only compose override (publishes ports for local testing) ---
docker-compose.override.yml

# --- IDE / OS ---
.idea/
*.iml
.vscode/
.DS_Store
